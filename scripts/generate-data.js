// Dataset generator is embedded in the build artifact generation used for the demo.
// Replace data/transactions.csv with real merchant data through the ingestion service in production.
console.log('Use the included 50,000-row synthetic dataset for the buildathon demo.');
